# NR155 USB composite (VID:PID 303a:4002). Udev delegates lifecycle to
# systemd; it never calls Docker itself.
ACTION=="add", SUBSYSTEM=="tty", ATTRS{idVendor}=="303a", ATTRS{idProduct}=="4002", GROUP="dialout", MODE="0660", TAG+="systemd", ENV{SYSTEMD_WANTS}+="nextwaves-gate-hotplug.service"
